#!/bin/bash
# Process the data files and run analysis

echo "Processing data files..."

# Concatenate the data files
cat ../data/base.dat ../data/data.dat > ../data/combined.dat

echo "Data files combined."
